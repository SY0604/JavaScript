// Convert a Number to a String: Write a function to convert a number to its string representation.
const numberToString = (num) => num.toString();

const numToConvert = 123;
console.log(`The string representation of ${numToConvert} is: "${numberToString(numToConvert)}"`);
